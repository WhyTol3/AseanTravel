<%= form_for(@comment_place) do |f| %>
  <% if @comment_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment_place.errors.count, "error") %> prohibited this comment_place from being saved:</h2>

      <ul>
      <% @comment_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
